import axios from 'axios';
import { LLMServiceError } from './errors';
function sleep(ms) {
    return new Promise(resolve => setTimeout(resolve, ms));
}
function computeBackoff(attempt, policy) {
    var _a;
    const base = (_a = policy.baseDelayMs) !== null && _a !== void 0 ? _a : 250;
    const jitter = Math.random() * base;
    const exp = base * Math.pow(2, Math.max(0, attempt - 1));
    return Math.min(exp + jitter, 5000);
}
export function createHttpClient(config) {
    if (!config.baseURL) {
        throw new Error('[@autolabz/llmapi-sdk] Missing required baseURL. Provide a full llmapi-service URL.');
    }
    const client = axios.create({
        baseURL: config.baseURL,
        headers: {
            ...(config.defaultHeaders || {}),
            'Content-Type': 'application/json',
        },
    });
    // 将 AuthBridge 挂载到实例，供非 axios 流式实现读取鉴权信息
    try {
        client.defaults._autolabAuth = config.auth;
    }
    catch { }
    client.interceptors.request.use(async (req) => {
        var _a, _b, _c;
        try {
            if (!((_a = req.headers) === null || _a === void 0 ? void 0 : _a['x-request-id'])) {
                req.headers = req.headers || {};
                req.headers['x-request-id'] = `${Date.now().toString(36)}-${Math.random().toString(36).slice(2, 8)}`;
            }
            const [token, clientId] = await Promise.all([
                Promise.resolve(config.auth.getAccessToken()),
                Promise.resolve(config.auth.getClientId()),
            ]);
            if (token && !((_b = req.headers) === null || _b === void 0 ? void 0 : _b.Authorization)) {
                req.headers = req.headers || {};
                req.headers.Authorization = `Bearer ${token}`;
            }
            if (clientId && !((_c = req.headers) === null || _c === void 0 ? void 0 : _c['X-Client-Id'])) {
                req.headers = req.headers || {};
                req.headers['X-Client-Id'] = clientId;
            }
        }
        catch (_e) {
            // ignore
        }
        return req;
    });
    client.interceptors.response.use((res) => res, async (error) => {
        var _a, _b, _c, _d, _f;
        const original = error.config;
        const status = (_a = error.response) === null || _a === void 0 ? void 0 : _a.status;
        if (status === 401 && original && !original._retry) {
            original._retry = true;
            try {
                const newAccessToken = await config.auth.refreshAccessToken();
                original.headers = original.headers || {};
                original.headers.Authorization = `Bearer ${newAccessToken}`;
                return client(original);
            }
            catch (_e) {
                try {
                    (_c = (_b = config.auth).onUnauthorized) === null || _c === void 0 ? void 0 : _c.call(_b);
                }
                catch { } // best-effort
                throw LLMServiceError.fromAxiosError(error);
            }
        }
        if ((status === 429 || status === 503 || status === 504) && original) {
            const policy = config.retry || {};
            const max = (_d = policy.maxAttempts) !== null && _d !== void 0 ? _d : 3;
            const attempt = ((_f = original._attempt) !== null && _f !== void 0 ? _f : 0) + 1;
            if (attempt <= max) {
                original._attempt = attempt;
                const delay = computeBackoff(attempt, policy);
                await sleep(delay);
                return client(original);
            }
        }
        if (error.isAxiosError) {
            throw LLMServiceError.fromAxiosError(error);
        }
        throw error;
    });
    return client;
}
