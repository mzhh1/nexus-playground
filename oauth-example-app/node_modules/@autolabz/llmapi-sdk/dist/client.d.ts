import type { LLMClientConfig, HealthStatus, ChatCompletionsRequest, ChatCompletionsResponse, StreamHandlers } from './types';
export declare class LLMAPIClient {
    readonly client: any;
    readonly auth: any;
    constructor(config: LLMClientConfig);
    health(): Promise<HealthStatus>;
    chat(body: ChatCompletionsRequest): Promise<ChatCompletionsResponse>;
    chatStream(body: ChatCompletionsRequest & {
        stream: true;
    }, handlers?: StreamHandlers): Promise<void>;
}
export declare function createLLMClient(config: LLMClientConfig): LLMAPIClient;
