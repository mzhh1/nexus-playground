name = "np-hono-backend"
main = "src/index.ts"
compatibility_date = "2024-02-01"

# Env vars for production: set in Cloudflare dashboard or via wrangler secret.
# Local: use .dev.vars (see .dev.vars.example)
[vars]
# Optional: LOG_LEVEL = "info"
